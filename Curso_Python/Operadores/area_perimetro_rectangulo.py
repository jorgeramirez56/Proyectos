print('*** Area y perimetro de un rectangulo ***')
# Solicitamos al usuaio los valores de base y altura
base = float(input('Introduce el valor de la base del rectngulo: '))
altura = float(input('Introduce el valor de la altura del ractanulo: '))
# Calculos 
area = base * altura 
perimetro = 2 * (base + altura)
# Imprimimos resultados 
print(f'El area del rectangulo es: {area}\nEl perimetro del rectangulo es: {perimetro}')

